server {
    listen 80;
    server_name localhost;

    location /index.txt {
        root /usr/share/nginx/html;
        default_type text/plain;
    }
}    

server {
    listen 8080;
    server_name localhost;

    location /index2.txt {
        root /usr/share/nginx/html;
        default_type text/plain;
    }
}    
